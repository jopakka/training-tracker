package fi.joonasniemi.trainingtracker.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// --- Focused Teal (Dark) ---
val TealPrimary = Color(0xFF008080)        // Main teal color
val MutedGoldAccent = Color(0xFFF2C94C)    // Accent for highlights/buttons
val DarkSlateText = Color(0xFFE1E1E1)      // Primary text, slightly off-white for comfort
val DarkSurface = Color(0xFF242424)        // Card and list item backgrounds
val DarkBackground = Color(0xFF1A1A1A)      // Overall screen background
val SoftRedError = Color(0xFFEB5757)       // A softer red for delete actions
val DarkTextOnPrimary = Color(0xFFFFFFFF)  // Text on teal/gold buttons
val MutedText = Color(0xFF9E9E9E)          // For labels and secondary info

// --- Focused Teal (Light) ---
val TealPrimaryLight = Color(0xFF006A6A)        // A slightly darker teal for better contrast on light backgrounds
val GoldAccentLight = Color(0xFFC09513)         // A strong gold for accents
val NearBlackText = Color(0xFF1B1B1B)           // Main text color
val WhiteSurface = Color(0xFFFDFDFD)           // For cards and list items
val OffWhiteBackground = Color(0xFFF5F5F5)       // Main screen background
val RedErrorLight = Color(0xFFBA1A1A)           // Standard M3 light theme error color
val WhiteTextOnPrimary = Color(0xFFFFFFFF)  // Text on teal/gold buttons
val GreyText = Color(0xFF757575)                // For labels and secondary info